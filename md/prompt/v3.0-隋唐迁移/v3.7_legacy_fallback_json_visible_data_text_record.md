# v3.7-preflight.85 fallback JSON 可见数据文本收口记录

完成日期：2026-07-06

## 目标

继续推进 `codex-v3.0-隋末唐初aiagent历史策略迁移总提示词.md` 的 v3.7 发布候选前置收口。本轮聚焦 legacy fallback JSON 数据自身可能直出的可见展示字段，降低默认隋唐数据加载失败、MapEditor 打开旧资源或 UI fallback 读取旧数据时出现二战题材名词的风险。

## 范围

本轮只修改 fallback JSON 的可见文本字段：

- `WWIIHexV0/Data/ardennes_v0_scenario.json`
- `WWIIHexV0/Data/ardennes_v02_regions.json`
- `WWIIHexV0/Data/unit_templates.json`
- `WWIIHexV0/Data/generals.json`

保留不变：

- JSON key、schemaVersion、id、rawValue、faction、templateId、coord、controller、supplyFaction。
- 默认隋唐 `wude_618` 加载入口和 MapEditor 默认资源桥。
- `Command` / `ZoneDirective` / `WarCommandExecutor` / `RuleEngine` 管线。
- 规则数值、AI 决策、存档字段和 Codable 结构。

## 实现摘要

- `ardennes_v0_scenario.json`：场景 `displayName`、`dataNotes`、初始单位 `name`、keyLocations `name` 和 map `cityName` 改为中性旧战局口径。
- `ardennes_v02_regions.json`：数据集 `displayName`、region `name` 和 city `name` 改为中性旧战局口径。
- `unit_templates.json`：legacy 模板 `displayName` 改为甲骑军、骑军、步卒军、弓弩军、拒马弩军和守军。
- `generals.json`：legacy 将领可见 `name`、`localizedName`、`rank` 和 `biography` 改为迁移期中性总管称谓。

## 并发子 Agent 结论

- `Popper` 只读扫描确认高价值 JSON 可见字段集中在 fallback 场景、分区、单位模板和将领数据；`faction/controller/supplyFaction` 等 schema/兼容字段不应在本轮改动。
- `Cicero` 只读检查指出 `.85` 需同步 `README.md`、`update_log.md`、`md/flow/flow.md`、`md/flow/flowchart.md`、`md/plan/plan.md` 与新阶段记录；不要改写 `.45/.54/.55` 等历史记录含义。
- `Russell` 只读复扫确认本轮 JSON 命中已覆盖，同时提示下一批仍有 `Faction.displayName`、`VictoryReason.displayName`、`DataLoader` 校验错误和静态 fallback 可见残留，建议后续单独收口。

## 轻量检查

- `jq empty WWIIHexV0/Data/ardennes_v0_scenario.json`：通过，无输出。
- `jq empty WWIIHexV0/Data/ardennes_v02_regions.json`：通过，无输出。
- `jq empty WWIIHexV0/Data/unit_templates.json`：通过，无输出。
- `jq empty WWIIHexV0/Data/generals.json`：通过，无输出。
- 聚焦扫描四个改动 JSON 的可见字段 legacy 残留：无命中。
- 本轮改动文件尾随空白扫描：无命中。
- 本轮改动文件冲突标记扫描：无命中。
- `git diff --check`：通过，无输出。

## 未执行

未跑本机 Xcode build、XCTest、UI test、模拟器、Probe、Smoke、Full、性能测试或 app / MapEditor 启动；按 `md/test/test.md` 当前规范，本机默认只做轻量检查，重验证需 GitHub Actions 或人工授权。

## 遗留风险

- 本轮未运行游戏或 MapEditor，未验证 fallback JSON 实际加载后的 UI 渲染。
- 仍有源码层可见兜底残留待后续切片处理，尤其是 `Faction.displayName`、`VictoryReason.displayName`、`DataLoader` 错误文本、静态 `GameState` / `MapState` fallback。
- 工作树已有大量未提交改动，本轮只追加 `.85` 范围内 JSON 与文档记录，未回滚或整理其他方向改动。
