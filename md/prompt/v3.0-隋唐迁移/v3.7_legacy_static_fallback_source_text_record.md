# v3.7-preflight.87 静态 fallback 源码可见文本收口记录

完成日期：2026-07-07

## 目标

继续推进 `codex-v3.0-隋末唐初aiagent历史策略迁移总提示词.md` 的 v3.7 发布候选前置收口。在 `.86` 已收口源码层 legacy 可见兜底文本后，本轮处理 `DataLoader.loadInitialGameState()` 最后兜底会进入的静态 `GameState.initial()` 与 `MapState.ardennesV0()` 可见文本。

## 范围

本轮只修改：

- `WWIIHexV0/Core/GameState.swift`
- `WWIIHexV0/Core/MapState.swift`

保留不变：

- scenario id、objective id、faction、坐标、地形、补给源、胜负规则、加载顺序。
- Codable 字段、存档格式、命令管线、RuleEngine、AI 决策。
- legacy 兼容函数名和 id。

## 实现摘要

- `GameState.initial()` 的最后兜底单位名改为中性旧战局东路/西路军队口径。
- `GameState.initial()` 的初始化战报改为“旧战局 fallback 已初始化。”。
- `MapState.ardennesV0()` 的最后兜底城邑、要塞和 objective 展示名改为中性旧战局口径。

## 并发子 Agent 结论

- 只读源码复扫确认 `GameState.initial()` 和 `MapState.ardennesV0()` 改完后，当前未发现普通玩家、MapEditor 状态消息或错误反馈会直接看到的二战/旧题材残留。
- 剩余 `VictoryRules`、`RegionVictoryRules`、`MockAIClient` 中的 `Bastogne / St. Vith` 等为兼容查找字面量，不是当前可见文本；如后续要彻底清理源码字面量，可改为 objective id 常量或兼容表。

## 轻量检查

- `swiftc -parse WWIIHexV0/Core/GameState.swift`：通过，无输出。
- `swiftc -parse WWIIHexV0/Core/MapState.swift`：通过，无输出。
- 聚焦扫描 `GameState.swift`、`MapState.swift` 中旧题材可见字符串：无命中。
- 本轮改动文件尾随空白扫描：无命中。
- 本轮改动文件冲突标记扫描：无命中。
- `git diff --check`：通过，无输出。

## 未执行

未跑本机 Xcode build、XCTest、UI test、模拟器、Probe、Smoke、Full、性能测试或 app / MapEditor 启动；按 `md/test/test.md` 当前规范，本机默认只做轻量检查，重验证需 GitHub Actions 或人工授权。

## 遗留风险

- 本轮未运行最后兜底加载路径，未验证地图渲染和战报显示。
- 规则兼容查找中仍保留旧 objective/name 字面量，不直接展示，但后续可做源码字面量去题材化。
- 工作树已有大量未提交改动，本轮只追加 `.87` 范围内源码与文档记录，未回滚或整理其他方向改动。
