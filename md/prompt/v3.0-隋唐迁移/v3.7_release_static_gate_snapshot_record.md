# v3.7-preflight.30 发布候选静态门禁快照记录

完成日期：2026-07-05

## 目标

把发布前检查面板从静态清单进一步推进为只读的 `GameState` 静态门禁快照，让人工在不启动重测试的前提下能看到当前内存局势、数据覆盖和审计记录规模。

## 实现范围

- `ReleaseChecklistView` 版本更新为 `天命开唐 Agent · v3.7-preflight.30`。
- 新增“静态门禁快照”区块，只读取当前传入的 `GameState`、`hasSavedGame` 和 `GameSaveStatus`。
- 快照展示剧本 ID、回合阶段、行动势力、胜负状态、地图 hex/州郡/邻接、目标/地点/补给源、军队/前线、方面/防区、外交档案、军令/朝堂/外交审计、归附善后记录、战报记录和存档反馈。
- “代码已接入”和“运行时未验证”清单同步说明该快照已经接入，但只代表只读状态展示。

## 关键边界

- 不改 `GameState`、存档 JSON schema、地图数据、规则、命令、AI 决策或执行管线。
- 不启动 app、不跑 AI 回合、不执行 Xcode build / XCTest / 模拟器 / UI 点击 / 性能测试。
- 不下载或核对 GitHub Actions artifact；该面板不是 Agent C 云端验收器。
- 不声称发布候选已通过运行时验证。

## 关键文件

- `WWIIHexV0/UI/ReleaseChecklistView.swift`
- `README.md`
- `md/flow/flow.md`
- `md/flow/flowchart.md`
- `md/plan/plan.md`
- `update_log.md`

## 轻量检查

- `swiftc -parse MapEditor/MapEditorDocument.swift MapEditor/MapEditorGameResourceBridge.swift MapEditor/MapEditorExporter.swift MapEditor/MapEditorViewModel.swift MapEditor/MapEditorView.swift MapEditor/MapEditorCanvasScene.swift WWIIHexV0/UI/ReleaseChecklistView.swift`：通过，无输出。
- `jq empty WWIIHexV0/Data/wude_618_scenario.json WWIIHexV0/Data/wude_618_regions.json WWIIHexV0/Data/suitang_generals.json WWIIHexV0/Data/suitang_power_profiles.json WWIIHexV0/Data/suitang_terrain_rules.json WWIIHexV0/Data/suitang_unit_templates.json`：通过，无输出。
- `plutil -lint WWIIHexV0.xcodeproj/project.pbxproj`：通过，`OK`。
- `git diff --check`：通过，无输出。
- 改动文件尾随空白扫描：无命中。
- 改动文件冲突标记扫描：无命中。
- 当前文档旧重测试口径扫描：无命中。

## 后续风险

- 未授权启动 iOS/macOS app，未验证发布检查 sheet 的真实布局、动态字体、点击入口或滚动体验。
- 静态快照只读取当前内存状态，不验证 AI 多回合、存档读写、外交交接、善后 UI 或地图叠加层运行效果。
- 完整发布候选仍需要授权 Xcode build、iOS/macOS 启动、观察者多回合、UI 点击烟测和云端 artifact 验收。
