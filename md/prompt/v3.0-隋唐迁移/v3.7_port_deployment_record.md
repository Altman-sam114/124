# v3.7-preflight.109 己控港口海港生产部署点记录

切片 ID：v3.7-preflight.109

来源：总提示词 §0.2 当前可执行队列中的“完整水战、港口补给源、港口部署点与粮道扩展规则”子问题。

当前基线：

- `origin/main` 最新提交：`0430867 rules: reduce controlled water crossing movement cost`。
- v3.7-preflight.106 已让己控渡口/港口/海港成为战术补给投送点。
- v3.7-preflight.108 已让己控渡口/港口/海港降低相邻非道路跨河移动成本。

目标：

- 让己方控制的港口/海港在没有合格州郡或永久补给源部署点时，作为新征发部队的安全后方部署 fallback。
- 复用现有生产完成链路，不新增命令、JSON 字段或 MapState 永久补给源。

非目标：

- 不实现永久港口补给源、完整水战、河面控制、水师专属部署或水路运输。
- 不让渡口 `ferry` 成为募兵部署点。
- 不改变生产成本、建造回合、经济收入、战术补给线、移动、战斗或部署防区权威。

允许修改文件：

- `WWIIHexV0/Rules/EconomyRules.swift`
- `WWIIHexV0/Tests/RuleEngineCoreTests.swift`
- `README.md`
- `update_log.md`
- `md/flow/flow.md`
- `md/flow/flowchart.md`
- `md/plan/plan.md`
- `md/prompt/v3.0-隋唐迁移/codex-v3.0-隋末唐初aiagent历史策略迁移总提示词.md`
- 本记录文件

实现摘要：

- `EconomyRules.deploymentHex` 在既有合格州郡和 `state.map.supplySources(for:)` fallback 之后，新增 `controlledPortDeploymentHexes`。
- 部署点只接受 `MapFeatureKind.port` / `harbor`，且必须可通行、己控、无驻军、非敌邻接。
- 若港口/海港为敌控，则生产订单保持在队列中等待后续安全部署点。
- 新增 `RuleEngineCoreTests.testControlledPortCanReceiveProducedDivisionWhenNoSupplySourceIsAvailable` 作为云端 XCTest 语义保护。

轻量检查要求：

- `swiftc -parse WWIIHexV0/Rules/EconomyRules.swift WWIIHexV0/Tests/RuleEngineCoreTests.swift`
- `git diff --check`
- 文档尾随空白扫描
- 冲突标记扫描

禁止项：

- 不跑本机 `xcodebuild build/test`、XCTest、模拟器、Probe、Smoke、Full、UI test 或性能测试。
- 不把本轮语义测试断言冒充为本机完整验收；重验证交给 GitHub Actions。

遗留风险：

- 未做运行时多回合生产和 UI 展示截图复核。
- 永久港口补给源、完整水战和水师部署仍待后续独立切片。
