# v3.7-preflight.92 阶段与旧总管展示口径收口记录

完成日期：2026-07-06

## 目标

在多轮玩家可见文案和记录净化之后，继续收口两个低风险可见残留点：

- `GamePhase.displayName` 仍把自动行动阶段显示为 `AI 行动` / `AI 军令`，会进入 HUD、开局引导、命令面板和 legacy prompt 自由文本。
- `general_agents.json` 中 legacy `guderian` 兼容配置的展示名仍是旧人物专名。

本轮只改展示口径，不改变回合阶段 rawValue、legacy id、势力 rawValue、单位 id、命令管线、AI 决策或规则执行。

## 改动范围

- `WWIIHexV0/Core/GamePhase.swift`
  - `.germanAI.displayName` 从 `AI 行动` 改为 `朝堂行动`。
  - `.aiCommand.displayName` 从 `AI 军令` 改为 `朝堂军令`。
  - 保留 `.germanAI` / `.aiCommand` rawValue 和 `allowsAIExecution` 逻辑。
- `WWIIHexV0/Data/general_agents.json`
  - legacy `guderian` 配置的 `name` 从 `古德里安` 改为 `历史总管`。
  - 保留 `id: "guderian"`、`faction: "germany"`、`commandStyle: "breakthrough"` 和 legacy `assignedDivisionIds`。

## 明确不改

- 不改 `GamePhase` case、rawValue、Codable、回合推进、玩家/自动行动判定或存档兼容。
- 不改 `general_agents.json` schema、legacy id、势力 rawValue、单位 id、command style 或 DataLoader 校验。
- 不处理 legacy objective 展示名 fallback、region fallback 胜负摘要或 `MarshalFrontSummary.status` 字符串枚举化；这些留给后续规则/AI 结构收口。
- 不跑本机 Xcode build / XCTest / 模拟器 / Probe / Smoke / Full。

## 轻量检查

- `command -v swiftc`：无输出，当前容器缺少 `swiftc`，未能执行 Swift 单文件语法检查。
- `command -v jq`：无输出，当前容器缺少 `jq`。
- `node -e 'JSON.parse(require("fs").readFileSync("WWIIHexV0/Data/general_agents.json", "utf8")); console.log("json ok")'`：通过，输出 `json ok`。
- 聚焦扫描 `AI 行动` / `AI 军令` / `"name": "古德里安"`：改动文件中不再命中。
- 本轮改动文件尾随空白扫描：无命中。
- 本轮改动文件冲突标记扫描：无命中。
- `git diff --check`：通过，无输出。

## 遗留风险

- 本轮未启动主游戏、未进入 HUD / 引导 / 命令面板，也未运行 legacy LocalLLM prompt。
- 单文件 parse 和 JSON 解析只覆盖语法与格式，不等同于 Xcode 构建或运行时验证。
- legacy objective 展示名 fallback 和 `MarshalFrontSummary.status` 字符串逻辑仍需后续单独收口。
