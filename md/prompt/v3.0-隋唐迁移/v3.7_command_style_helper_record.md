# v3.7-preflight.94 默认指挥风格共享 helper 收口记录

完成日期：2026-07-06

## 目标

在自动总管默认指挥风格与多势力映射对齐之后，继续处理 `.93` 留下的维护风险：`AppContainer` 与 `ZoneCommanderAgent` 各自维护一份相同的默认指挥风格 switch。后续若扩展势力性格，两处映射容易再次分叉。

本轮只抽取既有映射为共享 helper，不改变默认映射结果、directive schema、命令管线、规则执行、AI 阈值、战术选择函数、势力 rawValue 或存档格式。

## 改动范围

- `WWIIHexV0/Agents/ZoneCommanderAgent.swift`
  - 在 `ZoneCommanderAgentConfig.CommandStyle` 内新增静态 `defaultForFaction(_:)` helper。
  - `TheaterCommanderPool.defaultConfig(for:)` 改为调用该共享 helper。
  - 删除 `TheaterCommanderPool` 私有重复 `defaultCommandStyle(for:)`。
- `WWIIHexV0/App/AppContainer.swift`
  - `buildCommanderPool(state:registry:)` 改为调用 `ZoneCommanderAgentConfig.CommandStyle.defaultForFaction(_:)`。
  - 删除 `AppContainer` 私有重复 `defaultCommandStyle(for:)`。

## 明确不改

- 不新增 Swift 文件，不修改 `WWIIHexV0.xcodeproj/project.pbxproj`。
- 不改变唐、瓦岗、薛秦、刘武周、旧东路势力默认 `.aggressive`，洛阳隋、夏、东突厥、旧西路势力默认 `.balanced` 的映射。
- 不改 `ZoneCommanderAgentConfig.CommandStyle` case、rawValue、Codable 合同或 `.cautious` case。
- 不改 `Command` / `ZoneDirective` / `WarCommandExecutor` / `RuleEngine` 管线。
- 不改 `MarshalAgentConfig`、`DirectiveEnvelope`、`TheaterDirectiveEnvelope`、`WarDirectiveRecord` 或 `AgentDecisionRecord` schema。
- 不改 `Faction` case、rawValue、JSON 数据、DataLoader、胜负规则、补给规则或存档格式。
- 不跑本机 Xcode build / XCTest / 模拟器 / Probe / Smoke / Full。

## 并发子 Agent 审计

本轮主 Agent 启动一个只读子 Agent 复核 `.94` 边界。子 Agent 不写文件，只确认最小安全边界、文档同步范围、允许的轻量检查和潜在编译风险。主 Agent 负责实际代码与文档整合。

## 轻量检查

- `command -v swiftc`：无输出，当前容器缺少 `swiftc`，未能执行 Swift 单文件 parse。
- 源码聚焦扫描 `defaultCommandStyle(`：`WWIIHexV0/Agents/ZoneCommanderAgent.swift` 与 `WWIIHexV0/App/AppContainer.swift` 无命中。
- 源码聚焦扫描 `defaultForFaction`：命中共享 helper 定义与两个调用点。
- 聚焦扫描 `.93` 当前状态残留、`AppContainer.defaultCommandStyle` 和“宜抽共享 helper”：无命中。
- 本轮改动文件尾随空白扫描：无命中。
- 本轮改动文件行首冲突标记扫描：无命中。
- `git diff --check`：通过，无输出。

## 遗留风险

- 本轮未运行 AI 回合、方面总管生成链、directive 编译执行链或多势力自动回合。
- 单文件 Swift parse 未执行成功，因为当前容器缺少 `swiftc`。
