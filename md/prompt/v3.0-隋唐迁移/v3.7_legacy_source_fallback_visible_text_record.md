# v3.7-preflight.86 源码层 legacy 可见兜底文本收口记录

完成日期：2026-07-07

## 目标

继续推进 `codex-v3.0-隋末唐初aiagent历史策略迁移总提示词.md` 的 v3.7 发布候选前置收口。在 `.85` 已收口 fallback JSON 可见字段后，本轮处理源码层仍可能被 HUD、发布检查、胜负提示、MapEditor 或错误反馈直接读取的 legacy 可见兜底文本。

## 范围

本轮只修改：

- `WWIIHexV0/Core/Faction.swift`
- `WWIIHexV0/Core/VictoryState.swift`
- `WWIIHexV0/Data/DataLoader.swift`

保留不变：

- enum case、rawValue、Codable 字段、JSON key、id、schema、兼容查找。
- `guderian`、`bastogne*` 等 legacy id / case 名。
- 胜负判定逻辑、加载顺序、RuleEngine、命令管线、AI 决策和存档格式。

## 实现摘要

- `Faction.displayName` 的 `.germany` / `.allies` 改为“旧剧本东路势力 / 旧剧本西路势力”。
- `VictoryReason.displayName` 的 legacy 胜负原因改为中性旧战局目标、主力和断粮口径。
- `DataLoader.validate(...)` 中旧战局补给源和旧代理配置校验错误改为中性描述，同时保留旧 id 匹配语义。

## 并发子 Agent 分工

- 主线程实现 `.86` 三个 Swift 文件并更新文档。
- 只读 explorer 负责复扫剩余源码可见残留，排除 sanitizer 左值、id/rawValue/schema 和历史文档。
- 只读 explorer 负责检查 `.86` 文档链路、版本号、flowchart 节点和阶段记录口径。

## 轻量检查

- `swiftc -parse WWIIHexV0/Core/Faction.swift`：通过，无输出。
- `swiftc -parse WWIIHexV0/Core/VictoryState.swift`：通过，无输出。
- `swiftc -parse WWIIHexV0/Data/DataLoader.swift`：通过，无输出。
- 聚焦扫描三个 Swift 文件中旧题材可见词：剩余命中为类型名、enum case、函数名、兼容 id 或注释，不是本轮展示字符串。
- 本轮改动文件尾随空白扫描：无命中。
- 本轮改动文件冲突标记扫描：无命中。
- `git diff --check`：通过，无输出。

## 未执行

未跑本机 Xcode build、XCTest、UI test、模拟器、Probe、Smoke、Full、性能测试或 app / MapEditor 启动；按 `md/test/test.md` 当前规范，本机默认只做轻量检查，重验证需 GitHub Actions 或人工授权。

## 遗留风险

- 本轮未运行 HUD、发布检查、胜负提示或 MapEditor 错误反馈路径。
- 仍需继续复扫静态 `GameState` / `MapState` fallback、MapEditor legacy metadata 和其他非注释可见文本路径。
- 工作树已有大量未提交改动，本轮只追加 `.86` 范围内源码与文档记录，未回滚或整理其他方向改动。
