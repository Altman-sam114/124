# v3.7-preflight.110 己控港口海港战略补给源记录

切片 ID：v3.7-preflight.110

来源：总提示词 §0.2 当前可执行队列中的“完整水战、永久港口补给源与更细水域控制规则”子问题。

当前基线：

- `origin/main` 最新提交：`97b0268 rules: deploy production at controlled ports`。
- v3.7-preflight.106 已让己控渡口/港口/海港成为战术补给投送点。
- v3.7-preflight.109 已让己控港口/海港成为生产完成后的安全部署 fallback。

目标：

- 让己方控制的港口/海港所在州郡成为 region 战略层补给源。
- 复用 `RegionSupplyRules.strategicSupplySources`，不新增 JSON 字段或永久写入 `MapState.supplySources`。

非目标：

- 不实现完整水战、河面控制、水师运输或水师专属部署。
- 不让渡口 `ferry` 成为州郡级战略补给源。
- 不改变战术补给 anchor、经济收入、生产部署、移动、战斗或命令管线。

允许修改文件：

- `WWIIHexV0/Rules/RegionSupplyRules.swift`
- `WWIIHexV0/Tests/RegionSupplyRulesTests.swift`
- `README.md`
- `update_log.md`
- `md/flow/flow.md`
- `md/flow/flowchart.md`
- `md/plan/plan.md`
- `md/prompt/v3.0-隋唐迁移/codex-v3.0-隋末唐初aiagent历史策略迁移总提示词.md`
- 本记录文件

实现摘要：

- `RegionSupplyRules.strategicSupplySources` 增加 `controlledPortSupplySourceRegions`。
- 只接受 `MapFeatureKind.port` / `harbor`，且 marker 所在 hex 必须可通行、己控、所属 region 可通行。
- 新增 `RegionSupplyRulesTests.testControlledPortCanActAsStrategicSupplySource`，覆盖己控港口成为战略补给源、敌控港口不为己方提供战略补给。

轻量检查要求：

- `swiftc -parse WWIIHexV0/Rules/RegionSupplyRules.swift WWIIHexV0/Tests/RegionSupplyRulesTests.swift`
- `git diff --check`
- 文档尾随空白扫描
- 冲突标记扫描

禁止项：

- 不跑本机 `xcodebuild build/test`、XCTest、模拟器、Probe、Smoke、Full、UI test 或性能测试。
- 不把本轮语义测试断言冒充为本机完整验收；重验证交给 GitHub Actions。

遗留风险：

- 未做运行时 UI 和多回合战略补给复核。
- 完整水战、河面控制、水师部署和跨水域运输仍待后续独立切片。
